[package]
name = "hello"
version.workspace = true
edition.workspace = true
exclude = ["src/intentionally-broken-symlink"]

[build-dependencies]
byteorder.workspace = true

[dependencies]
byteorder = { workspace = true, features = ["std"] }

[dev-dependencies]
libc = { workspace = true, features = ["std"] }
